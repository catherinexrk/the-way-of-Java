输入char类型字符
sex = myScanner.next().charAt();

case中的的数值必须为 常量 1  'a'

break默认退出最近的循环体

continue结束下面循环的内容，重新返回循环开头进行判断是否接着循环进行


基本数据类型
    字符型
         char
    布尔型
        boolean
    数值型
        整形
            byte short int long
        浮点型
            float double

引用数据类型
    接口
    类
    数组

&& 前者对，后者不判断
||  前者对，后者不判单
&&i效率高 ||效率高


~按位取反
^按位异或
&按位相与
|按位相或

