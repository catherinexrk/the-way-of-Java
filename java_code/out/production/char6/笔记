面向对象设计
    类和对象设计
    类是抽象的、对象是具体的
    类是模板，对象是具体

    属性是类的组成部分
        属性可以基本数据类型，也可以是引用数据类型

对象
    属性
    行为

p1是对象名，new Person()才是真正的对象
p1是对象名，new Person()才是真正的对象

Person p1 = new Person()
Person p2 = p1;p2 指向p1，公共用同一片内存空间
    p1、p2是对象的引用

堆存储类中的变量地址
方法区(常量池)存档变量地址

把地址赋值给p中的数据初始化
数据存储

类是模板，对象是具体实现


类方法权限
    protected private default public


可变参数 = 数组 (int... nums)

